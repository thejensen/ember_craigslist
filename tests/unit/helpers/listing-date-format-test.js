
import { listingDateFormat } from 'js-ember-craigslist/helpers/listing-date-format';
import { module, test } from 'qunit';

module('Unit | Helper | listing date format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listingDateFormat([42]);
  assert.ok(result);
});

